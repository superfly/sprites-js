{
  "name": "@superfly/sprites-test-cli",
  "version": "0.1.0",
  "description": "Test CLI for Sprites JavaScript SDK",
  "type": "module",
  "bin": {
    "test-cli": "./dist/index.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/index.js",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}

